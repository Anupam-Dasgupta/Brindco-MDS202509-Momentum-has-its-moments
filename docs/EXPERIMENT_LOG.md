# Research decision log

## 2026-09-17 — Primary missing-signal rule and separate sensitivity

Before any shadow-risk estimate, portfolio-performance analysis, or holdout inspection, freeze `STRICT_AUDITABLE_SIGNAL_PRIMARY`: an official formation-date NIFTY 500 member is ineligible at that formation if any required t−12 through t−2 monthly economic return is unavailable. The exclusion is temporary and explicitly audited. Recompute the eligible count, top-decile seat count, ranks, and winners. This implements the complete-history eligibility requirement already stated in `../plan.md` §6; it does not value a missing entitlement or claim that the omitted security could not have won.

Run `ZERO_ENTITLEMENT_SENSITIVITY` as a separately stored selection-only scenario. Set only the 30 still-unresolved incremental event values to zero where an otherwise valid raw-price return and all ordinary observations exist. Keep accepted treatments and the four bonus-debenture daily exceptions unchanged. This is a modelling scenario, not an audited total-return history or an alternative primary strategy.

Reason: primary signals must be reproducible from auditable inputs, while the separate scenario exposes winner-set sensitivity to the unresolved events. This decision was made without viewing shadow or account performance, and before any holdout value read. Its limitations remain visible in `audits/SHADOW_PORTFOLIO_AUDIT.md`.

## 2026-09-17 — Resolve pre-coding volatility edge-case conflict

The first volatility-stage prompt specified full exposure at zero estimated volatility. Before running the overlay or inspecting any new risk results, this was identified as inconsistent with the frozen `../plan.md` §7.3. The user's correction confirmed that the plan governs: a missing, nonfinite, or nonpositive estimate receives a cash target and visible error status. Positive finite estimates alone use `min(1, 0.12 / sigma_hat)`. No risk result was used to choose this rule.

## 2026-09-17 — Modelling-shadow corporate-action continuation

Before account performance or holdout evaluation, freeze a narrow continuation rule for the separate development modelling shadow. The strict audited stock returns, strict shadow, accepted corporate-action treatments, primary signal, and winner sets remain unchanged. This replaces the earlier single-event modelling continuation without retroactively calling its return audited.

For a held day with unavailable strict return, use the observed raw NSE price return only when the sole unresolved component is a complex incremental entitlement, all known cash and ordinary-share effects are neutral, the event identities are complete, and the current and previous quotes, date, security identity, and eligible equity series are valid. This assumes **zero incremental entitlement value** for modelling only; it is not a conservative bound on volatility or performance. Record every actual use. Missing market data, ordinary mechanical actions, terminal outcomes, and mixed or ambiguous event groups still block.

HGS on 2022-02-22 is an explicit **verified ordinary combined action**, not an instance of that fallback. The official NSE action records identify a 1:1 bonus and ₹28 interim dividend on the same ex-date. HGS's 12 February 2022 exchange filing states that ₹28 is paid on pre-bonus shareholding and confirms the 1:1 bonus; its record date is 23 February 2022. The modelling shadow uses `(2 × ex-date close + ₹28) / previous close − 1`, with the dividend reinvested under the shadow's total-return convention. Source: https://www.bseindia.com/xml-data/corpfiling/AttachHis/f5386769-50d2-43c5-aa62-a7bee9caed11.pdf . This treatment is isolated from the unchanged strict audit series.

Rebuild the modelling shadow and [plan.md](../plan.md) §7.3 risk estimate through the development cutoff, with modelled security-days and affected 126-session risk windows visible. Any non-covered held return still stops the modelling shadow. If unresolved entitlements materially affect later results, [plan.md](../plan.md) §5.4 requires a provisional conclusion.

## 2026-09-17 — EASEMYTRIP verified combined-action continuation

Before any account-performance or holdout inspection, extend the separate modelling shadow's verified ordinary-action treatments to EASEMYTRIP on 2022-11-21. The company's 10 November 2022 exchange filing fixes the 22 November record date and states that each ₹2 equity share is split into two ₹1 shares, then three ₹1 bonus shares are issued for every one existing ₹1 share. The accepted NSE action records place both legs on the 21 November ex-date. One pre-event share therefore becomes eight post-event shares, with no cash. Use `(8 × ex-date close) / immediately preceding canonical close − 1` and an eightfold shadow-unit transformation. Do not use the raw price return. Source: https://www.easemytrip.com/investor-pdf/2022/Intimation-of-Record-Date-22-11-2022.pdf . This is a verified combined ordinary action in the modelling shadow, not an application of the zero-incremental-entitlement fallback. The strict audited artifacts remain unchanged. If a different held unavailable return appears, stop and report it.

## 2026-09-17 — Settlement evidence and actual account-event materiality

The earlier candidate preflight blocked an account run because payment/credit dates were missing and DHANI was a possible selected holding. The user clarified the frozen plan's account convention: known dividends are unspendable receivables, known bonus ratios are undeliverable economic claims until availability is evidenced, and a complex right blocks only an actually held account if wealth remains undefined. This correction was made before a complete account result or any holdout result was inspected.

Nine official NSE Clearing annual capital-market holiday notices plus two amendments establish the separate T+2 settlement-business-day calendar through 2023-03-31. The source list, hashes and dates are in `audits/SETTLEMENT_CALENDAR_AUDIT.md`. Development MOM and VM account paths started on 2015-04-01 and each have 709 valid NAV rows ending 2018-02-08. Both actually held DHANI on the 2018-02-09 rights ex-date (1,154 and 642 shares respectively). Its accepted theoretical signal value is not account cash or an evidenced account right valuation. Both paths stop before event-day NAV; neither reaches the development cutoff. The partial ledger's opening-day cash/stock/NAV components and exact blocker quantities were inspected for reconciliation; no complete account comparison, holdout output, or post-cutoff market value was inspected. No signal, winners, shadow, panel, or accepted corporate-action research artifact was changed. Further work must address this held account claim explicitly rather than silently zeroing or cashing out the right.
